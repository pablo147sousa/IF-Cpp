// Dados de ponto flutuante (float e double) em C e C++

/**
 *      Números de ponto flutuante representam os números
 * pertencentes ao conjunto dos Reais.
 *      A diferença entre float e double é que o ultimo
 * apresenta o dobro de tamanho em relação a float e por
 * isso possui uma maior precisão.
 *      O maior tipo que há é o long double, que possui
 * um tamanho de 12 Bytes.
 *      Os operadores aritméticos funcionam também para
 * números de ponto flutuante.
*/

#include <stdio.h>

int main()
{
    float real = 42.4242;
    return 0;
}